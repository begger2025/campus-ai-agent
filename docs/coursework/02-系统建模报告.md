# 校声智枢（Campus AI Agent）系统建模报告

> 《软件工程》期末大作业 · 交付物 2 / 9
>
> 项目名称：校声智枢 · Campus AI Agent —— 校园舆情感知与智能分析平台
> 团队成员：李泽勋（组长）、刘思雨、陈继橦、胡津
> 文档版本：v2.0（终版，全部模型与代码实测对齐）

## 修订历史

| 版本 | 阶段 | 变更说明 |
|------|------|----------|
| v1.0 | 第 1~2 周 | 初版：用例模型、数据模型类图（业务主线三级表） |
| v1.5 | 第 3~5 周 | 增补对话助手序列图、核心域类图、事件审核状态机 |
| v2.0 | 第 7 周 | 终版：证据核验子域建模、爬取队列状态机、模型—代码一致性映射全量复核 |

---

## 1. 引言

### 1.1 建模目的与方法

本报告以 UML 为建模语言，从四个互补视角刻画系统：**用例模型**（谁用系统做什么）、**静态结构模型**（类图：数据与模块如何组织）、**动态行为模型**（序列图：一次请求如何流经各层）、**状态模型**（状态机图：关键实体的生命周期）。

两条工程实践值得说明：

1. **图即代码（Diagram as Code）**：全部模型采用 Mermaid 文本语法编写，与源代码同仓库版本管理——模型变更走与代码相同的分支/提交/评审流程，可 diff、可回溯，从机制上避免"文档与代码两张皮"。
2. **模型可核对**：每张图在第 6 章给出源码锚点（文件/类/字段），所有状态取值（如事件的 `draft/published/rejected/archived`）均从代码中核实，而非示意性虚构。

### 1.2 建模范围

覆盖主系统（FastAPI 后端 + Vue 3 前端）、舆情核心算法包（`backend/agent/public_opinion_core`）、采集子系统（MediaCrawler 定制版）与证据核验子域。部署视角的建模（部署图、组件间物理拓扑）见《架构设计文档》，不在此重复。

---

## 2. 用例模型

### 2.1 参与者（Actors)

| 参与者 | 类型 | 说明 |
|--------|------|------|
| 游客 | 主要 | 未登录访问者，仅可浏览公开事件 |
| 普通用户（user） | 主要 | 注册学生：浏览、提问、评论、投稿、反馈 |
| 管理员（admin） | 主要 | 校方运营人员：审核、修正、选题、核验、运维 |
| 采集运维员 | 主要 | 执行爬虫与数据管线脚本的团队成员（命令行界面） |
| LLM 服务 | 次要（外部系统） | OpenAI 兼容接口 / 智谱 GLM，供研判、对话、联网检索 |
| 社交平台 | 次要（外部系统） | 微博/贴吧/知乎/快手/小红书，数据来源 |

### 2.2 用例图

UML 用例图按三个功能域给出（Mermaid 以 flowchart 语法呈现，椭圆语义用圆角节点表达；系统边界用子图表达）：

**图 2-1 公众与用户侧用例**

```mermaid
flowchart LR
    guest(["👤 游客"])
    user(["👤 普通用户"])
    llm(["🤖 LLM 服务"])

    subgraph sys["校声智枢系统"]
        uc1(浏览公开事件列表/详情)
        uc2(注册 / 登录)
        uc3(查看首页态势仪表盘)
        uc4(查看数据分析/舆情关注)
        uc5(对话式舆情问答)
        uc5a(多步检索推理 ReAct)
        uc5b(引用溯源与 Critic 复核)
        uc6(评论事件 / 举报评论)
        uc7(投稿线索 / 查询我的投稿)
        uc8(提交反馈)
    end

    guest --> uc1
    guest --> uc2
    user --> uc1
    user --> uc3
    user --> uc4
    user --> uc5
    user --> uc6
    user --> uc7
    user --> uc8
    uc5 -.include.-> uc5a
    uc5 -.include.-> uc5b
    uc5a --> llm
    uc5b --> llm
```

**图 2-2 管理侧用例**

```mermaid
flowchart LR
    admin(["👤 管理员"])
    llm(["🤖 LLM 服务"])

    subgraph adminsys["校声智枢系统 - 管理后台"]
        a1(查看后台概览/管线健康)
        a2(审核事件：发布/驳回/归档)
        a3(修正事件：重命名/合并/增删帖/软删除)
        a4(人工创建/删除事件)
        a5(智能选题：获取下轮采集关键词)
        a6(发起证据核验任务)
        a7(审核证据条目并交付入库)
        a8(审核投稿 / 管控评论)
        a9(用户封禁/启用)
        a10(查询审计日志)
    end

    admin --> a1
    admin --> a2
    admin --> a3
    admin --> a4
    admin --> a5
    admin --> a6
    admin --> a7
    admin --> a8
    admin --> a9
    admin --> a10
    a2 -.include.-> a10
    a6 --> llm
```

**图 2-3 采集与数据管线用例**

```mermaid
flowchart LR
    op(["👤 采集运维员"])
    plat(["🌐 社交平台"])
    llm(["🤖 LLM 服务"])

    subgraph pipeline["采集与数据管线"]
        c1(按关键词多平台采集)
        c2(多机队列协同采集)
        c3(同步原始帖 raw_posts)
        c4(清洗生成 processed_posts)
        c5(生成舆情事件)
        c5a(语义聚类)
        c5b(LLM 精修/合并/风险/生命周期)
        c6(构建离线语义向量)
        c7(运行评测基准与消融实验)
    end

    op --> c1
    op --> c2
    op --> c3
    op --> c4
    op --> c5
    op --> c6
    op --> c7
    c1 --> plat
    c2 -.include.-> c1
    c5 -.include.-> c5a
    c5 -.include.-> c5b
    c5b --> llm
```

### 2.3 关键用例规约

**UC-01 对话式舆情问答**（对应 FR-E1~E9）

| 条目 | 内容 |
|------|------|
| 参与者 | 普通用户 / 管理员 |
| 前置条件 | 已登录；系统中存在已发布事件数据 |
| 主流程 | 1. 用户在"舆情助手"页输入问题 → 2. 意图路由判定问题类型（事件查询/统计/闲聊）→ 3. 语义检索召回相关事件与帖子 → 4. 需要时 LLM 裁决"问的是否为该事件"→ 5. ReAct 循环补充检索 → 6. 生成带编号引用的结论 → 7. Critic 二次核查 → 8. 流式返回答案 |
| 扩展流程 | 2a. 路由规则未命中 → LLM 兜底分类；5a. LLM 不可用 → 规则通道直接生成确定性简报；6a. 检索无结果 → 明确告知"暂无相关数据"而非编造 |
| 后置条件 | 提问写入 chat_query_log（成为智能选题信号）；用量计入统计 |
| 验收基准 | 26 题金标基准 63/63，引用合法率 100%，P50 14.1s |

**UC-02 事件审核发布**（对应 FR-G2~G4）

| 条目 | 内容 |
|------|------|
| 参与者 | 管理员 |
| 前置条件 | 数据管线已生成 `draft` 状态事件 |
| 主流程 | 1. 管理员在"事件审核"页查看待审事件 → 2. 核对成员帖与研判结果 → 3. 必要时修正（重命名/合并/增删帖）→ 4. 置为 `published` → 5. 系统写入 EventReviewLog 审核日志 → 6. 事件对游客可见 |
| 扩展流程 | 4a. 内容不实 → `rejected`；4b. 已过时 → `archived`；3a. 人工修正后事件加 `curated` 锁，后续自动管线不覆盖人工结论 |
| 后置条件 | 每次状态变更均有审计记录；删除事件时级联清理成员链接与审核日志 |

**UC-03 证据核验**（对应 FR-H1~H5）

| 条目 | 内容 |
|------|------|
| 参与者 | 管理员；LLM 服务（外部） |
| 前置条件 | 存在需要事实核查的事件 |
| 主流程 | 1. 管理员发起证据采集 run → 2. 多提供方并行联网检索 → 3. URL 规范化 + SHA-256 去重 → 4. 抓取核验（先过 SSRF 内网拦截闸）→ 5. 管理员逐条审核证据 → 6. 批量交付入库进入数据管线 |
| 扩展流程 | 2a. 单一提供方失败 → 隔离该路，其余照常；4a. 目标为内网/环回地址 → 拒绝抓取并标记 needs_review；5a. 来源不在域名白名单 → 不允许交付 |
| 后置条件 | 证据与事件关联，Agent 回答可引用权威信源 |

---

## 3. 静态结构模型（类图）

### 3.1 数据模型类图

**图 3-1 业务主线与用户参与域**（`backend/models.py`、`backend/admin_models.py`）

```mermaid
classDiagram
    class RawPost {
        +int id
        +str platform
        +str external_id
        +str title / content / author
        +datetime publish_time
        +str url
        唯一约束(platform, external_id)
    }
    class ProcessedPost {
        +int id
        +int raw_post_id FK唯一
        +str content_clean
        +float heat_score
        +float heat_rank
        +str sentiment / risk_level
        +bool excluded 软删除
    }
    class PublicEvent {
        +int id
        +str event_key 唯一
        +str title / summary
        +str status draft/published/rejected/archived
        +str risk_level / lifecycle
        +bool curated 人工修正锁
        +int source_post_id FK
    }
    class EventPostLink {
        +int event_id FK
        +int processed_post_id FK
        +int raw_post_id FK
    }
    class User {
        +int id
        +str username 唯一
        +str role user/admin
        +str status
    }
    class EventComment {
        +int id
        +int event_id FK
        +int parent_id 一层回复
        +str status visible/hidden
        +int report_count
    }
    class UserSubmission {
        +int id
        +str status pending/approved/rejected
        +str image_path
    }
    class UserFeedback

    RawPost "1" -- "0..1" ProcessedPost : 清洗
    ProcessedPost "1" -- "*" EventPostLink
    PublicEvent "1" -- "*" EventPostLink : 多对多联结
    PublicEvent "1" -- "*" EventComment
    User "1" -- "*" EventComment
    User "1" -- "*" UserSubmission
    User "1" -- "*" UserFeedback
    UserSubmission ..> RawPost : 审核通过写回
```

**图 3-2 运营审计域**（`backend/admin_models.py`）

```mermaid
classDiagram
    class CrawlTask {
        +str platform / keyword
        +str status
        +datetime finished_at
    }
    class AgentRunLog {
        +str run_type
        +str result_summary
    }
    class EventReviewLog {
        +int event_id FK→public_events
        +str from_status / to_status
        +str operator / reason
    }
    class AdminOperationLog {
        +str operator / action / target
    }
    class SystemLog {
        +str level / message
    }
    PublicEvent "1" -- "*" EventReviewLog : 每次状态变更留痕
```

**图 3-3 证据核验子域**（`backend/models_evidence.py`，六表独立子域）

```mermaid
classDiagram
    class EvidenceRun {
        +int id
        +str status
        +str event_ref
    }
    class EvidenceQuery {
        +int run_id FK
        +str provider / query_text
    }
    class EvidenceDocument {
        +int query_id FK
        +str canonical_url
        +str canonical_url_hash SHA256唯一
    }
    class EvidenceItem {
        +int run_id FK
        +int document_id FK
        +str review_status
    }
    class EvidenceVerification {
        +int item_id FK
        +str status
        +str reasons
    }
    class EvidenceDeliveryBatch {
        +int run_id FK
        +int raw_post_id FK→raw_posts
    }

    EvidenceRun "1" -- "*" EvidenceQuery
    EvidenceQuery "1" -- "*" EvidenceDocument
    EvidenceDocument "1" -- "*" EvidenceItem
    EvidenceRun "1" -- "*" EvidenceItem
    EvidenceItem "1" -- "*" EvidenceVerification : 引用核验记录
    EvidenceRun "1" -- "*" EvidenceDeliveryBatch
    EvidenceDeliveryBatch ..> RawPost : 交付入数据管线
```

### 3.2 分层与核心域类图

**图 3-4 后端分层结构**（接口层 → 业务层 → 核心算法层，依赖单向向下）

```mermaid
classDiagram
    namespace Layer1_Routers {
        class auth_router
        class api_router
        class agent_public_router
        class admin_routers
        class ugc_routers
    }
    namespace Layer2_Services {
        class opinion_chat_service
        class event_curation
        class event_read_model
        class ugc_services
        class evidence_pkg
        class llm_client
        class embedding
    }
    namespace Layer3_OpinionCore {
        class core_service
        class semantic_clustering
        class llm_suite
        class scoring_suite
        class memory
        class adapter
    }
    note for admin_routers "admin / admin_events / admin_evidence"
    note for ugc_services "comment / submission service"
    note for llm_suite "llm_refine merge risk lifecycle"
    note for scoring_suite "scoring recency platform_weights"
    agent_public_router --> opinion_chat_service
    admin_routers --> event_curation
    admin_routers --> evidence_pkg
    api_router --> event_read_model
    opinion_chat_service --> core_service
    opinion_chat_service --> llm_client
    event_curation --> memory : 尊重 curated 锁
    core_service --> semantic_clustering
    core_service --> llm_suite
    core_service --> scoring_suite
    core_service --> adapter
    semantic_clustering ..> embedding : 向量由外层注入
```

设计要点（详细论证见《架构设计文档》）：

- **核心算法层仅依赖 Python 标准库**：`public_opinion_core` 不 import FastAPI/SQLAlchemy/torch，embedding 向量由外层注入（依赖倒置），因此该层可独立测试、可整包移植（本项目双仓同步正是利用了这一点）。
- **OpinionNote 便携数据类**是层间数据契约：数据库行经 `adapter` 转为 `OpinionNote` dataclass 后进入算法层，算法层对存储形态零感知。

---

## 4. 动态行为模型（序列图）

### 4.1 对话式舆情问答（UC-01 主流程）

**图 4-1** `POST /api/agent/public/chat/stream`

```mermaid
sequenceDiagram
    actor U as 用户
    participant FE as 前端 AgentChatView
    participant R as agent_public 路由
    participant CS as opinion_chat_service
    participant IR as intent_router
    participant SEM as semantic_posts/event_read_model
    participant EJ as event_judge
    participant RL as react_loop
    participant LLM as llm_client(外部LLM)
    participant CR as critic

    U->>FE: 输入问题
    FE->>R: POST /chat/stream (JWT)
    R->>CS: chat(question, session)
    CS->>IR: 意图分类
    IR-->>CS: 事件查询(规则命中)
    Note over IR: 规则未命中时降级 LLM 分类
    CS->>SEM: 语义检索(离线向量+余弦)
    SEM-->>CS: 候选事件+帖子
    CS->>EJ: LLM 裁决"问的是这件事吗"
    EJ-->>CS: 匹配事件
    CS->>RL: ReAct 多步循环(带预算上限)
    RL->>LLM: 推理/工具调用
    LLM-->>RL: 观察值
    RL-->>CS: 结论草稿+引用编号
    CS->>CR: Critic 二次事实核查
    CR-->>CS: 校正后结论
    CS-->>FE: SSE 流式分块返回
    CS->>CS: 写 chat_query_log(智能选题信号)
    Note over CS,LLM: LLM 不可用→规则通道生成确定性简报(降级不失败)
```

### 4.2 舆情事件生成管线（UC 数据管线核心）

**图 4-2** `scripts/generate_public_events.py`

```mermaid
sequenceDiagram
    actor OP as 采集运维员
    participant GEN as generate_public_events
    participant DB as MySQL/SQLite
    participant AD as core.adapter
    participant EMB as embedding(bge-small-zh)
    participant SC as core.semantic_clustering
    participant REF as core.llm_refine/llm_merge
    participant RISK as core.llm_risk/llm_lifecycle
    participant SCORE as core.scoring/recency/platform_weights
    participant MEM as core.memory
    participant PB as core.payload_builder

    OP->>GEN: 执行脚本
    GEN->>DB: 读 processed_posts(未剔除)
    GEN->>AD: 行→OpinionNote(清洗/解析"1.2万")
    GEN->>EMB: 文本→向量(本地快照加载)
    GEN->>SC: 注入向量聚类(余弦阈值)
    SC-->>GEN: 事件簇
    GEN->>REF: LLM 精修(拆大桶/具体化标题)+近重合并
    Note over REF: 并发执行且结果逐位不变<br/>LLM 失败→保留原簇
    GEN->>RISK: 事件级风险/生命周期研判
    Note over RISK: 非法输出(如"dormant")→该事件退回未研判
    GEN->>SCORE: heat_rank×平台权重×时效衰减
    GEN->>MEM: 对齐历史事件(跨运行ID稳定)
    Note over MEM: curated 人工修正锁优先, 不被覆盖
    GEN->>PB: 构造入库 payload
    PB->>DB: upsert public_events + event_post_links(draft 待审)
    GEN-->>OP: 输出统计与 warnings
```

### 4.3 证据核验全链路（UC-03）

**图 4-3** 证据采集 → 核验 → 人审 → 交付

```mermaid
sequenceDiagram
    actor AD as 管理员
    participant FE as AdminEvidenceView
    participant API as admin_evidence 路由
    participant COL as evidence.collector
    participant PRV as providers(多家LLM联网检索)
    participant CAN as canonicalize
    participant VER as verification
    participant WEB as 目标网站
    participant DLV as review_delivery

    AD->>FE: 发起证据采集
    FE->>API: POST /admin/evidence/runs
    API->>COL: 创建 run 并执行
    par 多提供方并行
        COL->>PRV: 检索(提供方A)
        COL->>PRV: 检索(提供方B)
    end
    Note over PRV: 单一提供方失败→隔离该路
    PRV-->>COL: 候选证据(URL+摘要)
    COL->>CAN: URL 规范化
    CAN-->>COL: canonical_url_hash(SHA-256 去重)
    COL->>VER: 引用核验
    VER->>VER: SSRF 闸: 解析主机,拦内网/环回/云元数据
    alt 地址安全
        VER->>WEB: 抓取正文比对
        WEB-->>VER: 页面内容
    else 内网地址
        VER-->>COL: needs_review(拒绝抓取)
    end
    AD->>API: PATCH items/{id}/review 逐条人审
    AD->>API: POST runs/{id}/deliver
    API->>DLV: 批量交付(域名白名单校验)
    DLV-->>API: 写 EvidenceDeliveryBatch→raw_posts
```

---

## 5. 状态模型（状态机图）

### 5.1 舆情事件审核状态机

**图 5-1** `PublicEvent.status`（取值与 `admin_events.VALID_EVENT_STATUSES` 一致）

```mermaid
stateDiagram-v2
    [*] --> draft : 数据管线生成/人工创建
    draft --> published : 管理员审核通过
    draft --> rejected : 内容不实/不宜公开
    published --> archived : 事件平息归档
    published --> rejected : 发布后发现问题撤下
    rejected --> draft : 修正后重审
    archived --> [*]
    note right of published
        仅 published 事件对游客可见、
        可评论(comment_service 校验)
    end note
    note right of draft
        每次状态迁移写 EventReviewLog
        (from_status→to_status+操作人+理由)
    end note
```

### 5.2 用户投稿状态机

**图 5-2** `UserSubmission.status`

```mermaid
stateDiagram-v2
    [*] --> pending : 用户提交(频控 429 拦截刷稿)
    pending --> approved : 管理员通过→写回 raw_posts 进入管线
    pending --> rejected : 管理员驳回
    approved --> [*]
    rejected --> [*]
    note right of pending
        并发双审防护:
        仅 pending 可审, 数据库约束兜底,
        重复审核返回 409 而非二次生效
    end note
```

### 5.3 评论状态机

**图 5-3** `EventComment.status`

```mermaid
stateDiagram-v2
    [*] --> visible : 用户发表(10 秒频控)
    visible --> hidden : 举报计数原子递增≥5 自动隐藏
    visible --> hidden : 管理员手动隐藏
    hidden --> visible : 管理员复核恢复
    note right of hidden
        自动隐藏写明 hidden_reason
        (举报满 5 条自动隐藏, 待管理员复核)
        阈值判断幂等, 并发重复置位无害
    end note
```

### 5.4 分布式爬取队列任务状态机

**图 5-4** `crawl_task_queue.status`（多机协同核心）

```mermaid
stateDiagram-v2
    [*] --> pending : seed_crawl_queue 播种
    pending --> claimed : worker 行级条件UPDATE抢占(仅命中1行才算成功)
    claimed --> done : 采集完成(记录 items_stored)
    claimed --> failed : 采集异常(记录 stop_reason)
    claimed --> pending : 租约到期自动回收(lease_expires_at 已过期)
    failed --> pending : reset_crawl_queue 人工回收重试
    done --> [*]
    note right of claimed
        无 MySQL 8.0 SKIP LOCKED 依赖,
        用条件 UPDATE 保证两台机器
        绝不认领同一任务
    end note
```

### 5.5 事件生命周期研判标签

**图 5-5** LLM 生命周期研判（`LLM_LIFECYCLES` 枚举），注意这是**研判标签**而非人工流转状态，随每轮管线重新评估：

```mermaid
stateDiagram-v2
    state "未研判" as unjudged
    [*] --> unjudged : 事件新生成
    unjudged --> ongoing : LLM 判定仍在发酵
    unjudged --> resolved : LLM 判定已解决/平息
    unjudged --> not_applicable : 不适用生命周期概念
    ongoing --> resolved : 下轮管线重评
    resolved --> ongoing : 出现新增讨论(复燃)
    note right of unjudged
        防御性设计: LLM 输出枚举外状态
        (如 dormant)或缺失理由时,
        该事件退回未研判(权重因子 1.0),
        绝不让编造状态进入生产数据
    end note
```

---

## 6. 模型—代码一致性映射

每张图均可在源码中逐一核对（这也是本报告"模型准确反映系统结构"的证据）：

| 图 | 源码锚点 |
|----|----------|
| 图 2-1~2-3 用例图 | `frontend/src/router/index.js`（角色守卫）、`backend/routers/*`（端点）、`scripts/*`（管线用例） |
| 图 3-1 业务域类图 | `backend/models.py`（RawPost/ProcessedPost/PublicEvent/EventPostLink/EventComment/UserSubmission） |
| 图 3-2 审计域类图 | `backend/admin_models.py`（User/CrawlTask/EventReviewLog/AdminOperationLog/SystemLog 等） |
| 图 3-3 证据子域类图 | `backend/models_evidence.py`（六表） |
| 图 3-4 分层类图 | `backend/routers/` → `backend/services/` → `backend/agent/public_opinion_core/` 目录结构与 import 方向 |
| 图 4-1 问答序列图 | `backend/services/opinion_chat_service.py`、`intent_router.py`、`event_judge.py`、`react_loop.py`、`critic.py`、`citations.py` |
| 图 4-2 管线序列图 | `scripts/generate_public_events.py`、`public_opinion_core/{adapter,semantic_clustering,llm_refine,llm_merge,llm_risk,llm_lifecycle,scoring,recency,platform_weights,memory,payload_builder}.py` |
| 图 4-3 证据序列图 | `backend/services/evidence/{collector,providers,canonicalize,verification,review_delivery,scope_policy}.py` |
| 图 5-1 事件状态机 | `backend/routers/admin_events.py`（`VALID_EVENT_STATUSES = {"draft","published","rejected","archived"}`） |
| 图 5-2 投稿状态机 | `backend/services/submission_service.py`（pending/approved/rejected、409 并发守卫） |
| 图 5-3 评论状态机 | `backend/services/comment_service.py`（visible/hidden、`AUTO_HIDE_REPORTS = 5`） |
| 图 5-4 队列状态机 | `MediaCrawler/store/crawl_queue.py`（pending/claimed、租约回收 SQL） |
| 图 5-5 生命周期标签 | `public_opinion_core/llm_lifecycle.py`（`LLM_LIFECYCLES = ("resolved","ongoing","not_applicable")`） |

---

## 7. 建模工具与维护约定

- **工具链**：Mermaid（文本化 UML），VS Code / Typora 实时预览，GitHub/GitLab 原生渲染；
- **维护约定**：修改涉及库表结构、状态枚举、层间依赖的代码时，同一 PR 内同步更新对应模型图（列入代码评审检查项）；
- **一致性保障**：状态枚举类模型（图 5-1~5-5）的取值直接引用代码常量，评审时以代码为准核对，防止模型腐化。

---

*本报告全部 13 张模型图基于终版代码绘制；第 6 章映射表可用于逐图核对。*
