# 第二周前端 → 后端/AI/爬虫 依赖事项清单

> 生成日期：2026-06-02  
> 用途：转交后端负责人、Agent 负责人、爬虫负责人，明确前端当前阻塞点  
> 依据：`第二周计划.md`（项目根目录）中"4 号同学：统一前端与课堂展示"任务

---

## 一、计划任务 vs 实际完成情况

对照第二周计划中 4 号同学（前端）的全部 5 项任务：

| #   | 计划任务                                    | 当前状态         | 差距分析                                                      |
| --- | --------------------------------------- | ------------ | --------------------------------------------------------- |
| 1   | **舆情分析页接入 GET /events**                 | ✅ 已完成 (Mock) | OpinionView 已重写为三栏布局，mock 数据驱动，接口切换点已预留                   |
| 2   | **个人事项页接入 POST /agent/personal/impact** | ✅ 已完成 (Mock) | PersonalView 新增事件影响区块，通过 `?event_id=xxx` 接收联动，mock 三级影响数据 |
| 3   | **增加联动入口（事件详情→"查看对我的影响"）**              | ✅ 已完成        | EventDetailView 已创建，路由 `/events/:id` 已注册，联动链路已打通          |
| 4   | **保留 mock 降级 + 数据来源标识**                 | ✅ 已完成        | DataSourceBadge 组件已集成到全部 6 个页面；API 层保留 mock 降级            |
| 5   | **准备课堂演示路径**                            | ⏳ 待完成        | 核心链路已跑通（mock），演示脚本/文档待编写                                  |

### 飞书 FE 任务与计划的对应关系

| 飞书 FE 工作包      | 计划是否提及                | 当前状态         | 建议                         |
| -------------- | --------------------- | ------------ | -------------------------- |
| FE-01 路由导航权限   | ✅ 计划隐含（前端骨架）          | ✅ 完成         | —                          |
| FE-02 API 请求层  | ❌ 未单独提及               | ✅ 已完成        | 拦截器需后端确认 code 枚举后定稿        |
| FE-03 登录页      | ✅ 计划隐含（用户系统）          | ✅ 完成 (Mock)  | —                          |
| FE-04 舆情工作台    | ✅ 任务 1：接入 GET /events | ✅ 已完成 (Mock) | 三栏布局完整，预留接口切换点             |
| FE-05 事件列表/详情  | ✅ 任务 3：联动入口           | ✅ 已完成        | EventDetailView 已创建，联动链路已通 |
| **FE-06 后台概览** | ❌ **计划只字未提**          | ❌ 未开始        | 建议第三周                      |
| **FE-07 事件审核** | ❌ **计划只字未提**          | ❌ 未开始        | 建议第三周                      |
| **FE-08 数据管理** | ❌ **计划只字未提**          | ❌ 未开始        | 建议第三周                      |
| **FE-09 运维反馈** | ❌ **计划只字未提**          | ❌ 未开始        | 建议第三周                      |
| FE-10 联调验收     | ✅ 计划 Day 12~Day 14    | ⏳ 待后端就绪      | 核心链路 mock 已跑通              |

---

## 二、计划本身的分析与建议（更新于 2026-06-02 22:00）

### ⚠️ 问题 1：FE-06~FE-09（管理员后台）不在第二周计划中

第二周计划明确划定了范围边界：

> **本周暂不追求**：不追求…不大规模重做 UI 风格。不一次性重构所有独立 Agent 代码。

而飞书 FE-06~FE-09 拆出了 4 个完整的管理后台页面。计划聚焦的是**用户侧核心链路**：
`首页 → 舆情分析(事件列表) → 事件详情 → 个人影响 → 个人建议`

管理员后台的 4 个页面工作量接近翻倍前端总工作量。**建议**：管理员后台推迟到第三周，第二周集中精力跑通用户侧联调。

### ✅ 问题 2：FE-04 工作量 — 已解决

~~计划描述为"舆情分析页接入真实事件接口"，但当前 OpinionView.vue 只是一个 PageStub 占位壳。~~

OpinionView 已重写为完整三栏布局（热点事件列表 / 事件研判详情 / Agent 问答），mock 数据驱动，接口切换点已预留。

### ✅ 问题 3：EventDetailView — 已解决

~~EventDetailView.vue 文件不存在，路由 `/events/:id` 指向的是 EventListView 本身。~~

EventDetailView 已创建，路由已正确指向。联动链路 `EventDetailView → PersonalView(?event_id=xxx)` 已打通。

### ✅ 补充建议：FE-02 — 已完成

统一 axios 实例 `src/api/http.js` 已创建，含请求/响应拦截器、token 注入、统一错误处理、401/403 自动跳转。⚠️ 拦截器需后端确认 `code` 枚举值和 401/403 格式后定稿。

---

## 三、计划前端任务的精确时间线对照

| 计划 Day     | 计划描述          | 前端当前可做                 | 依赖                                              |
| ---------- | ------------- | ---------------------- | ----------------------------------------------- |
| Day 8      | 整合准备 + 启动会    | —                      | 全员参与                                            |
| Day 9      | 数据与数据库准备      | —                      | 2号同学                                            |
| Day 10     | 公共舆情 Agent 接入 | 无                      | 1号+3号同学                                         |
| Day 11     | 个人 Agent 接入   | 无                      | 1号+3号同学                                         |
| **Day 12** | **前端联调**      | **预留 mock 数据，独立完成 UI** | **需要 GET /events, POST /agent/personal/impact** |
| Day 13     | 测试修整 + 文档     | 联调 bug 修复              | 全员                                              |
| Day 14     | 总结 + 演示       | 准备演示路径                 | 全链路                                             |

---

## 四、按后端工作包分类的依赖清单

### 后端工作包 3：统一 API 返回结构

**阻塞：FE-02, FE-04, FE-05, 所有页面**

| 需要确认                      | 影响            |
| ------------------------- | ------------- |
| `code/message/data` 的标准格式 | 前端无法编写统一响应拦截器 |
| `code` 成功/失败的枚举值          | 无法统一处理成功/失败逻辑 |
| 401/403 场景的返回码和格式         | 无法编写自动登出/跳转逻辑 |

**建议：** 请提供一个示例请求-响应文档（含成功和失败两种 case），前端据此搭建 `src/api/http.js`。

---

### 后端工作包 5：Agent 分析 + 事件接口 （🔴 最高优先级，阻塞 Day 12 联调）

**阻塞：FE-04, FE-05, 计划任务 1/2/3**

| 接口                                | 请求/响应字段                                                                                                                                                                                                  | 阻塞任务                |
| --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- |
| `GET /api/events`                 | 入参：`status=published`、`keyword`、`risk_level`、分页；出参：`{ items: [{ id, title, summary, topic, sentiment, risk_level, heat_score, confidence, source_count, status, created_at }], total, page, page_size }` | 计划任务 1：舆情分析页        |
| `GET /api/events/{id}`            | 出参：事件完整详情 + `source_post_ids[]` + `affected_scope[]`                                                                                                                                                     | 计划任务 3：事件详情页 + 联动入口 |
| `POST /api/agent/public/analyze`  | 入参：`keyword`, `sources[]`；出参：`{ events: [...], summary_text }`                                                                                                                                           | FE-04 Agent 问答面板    |
| `POST /api/agent/personal/impact` | 入参：`{ user_id, event_signal: {...}, schedule: [], todos: [], preferences: {}, current_time }`；出参：`{ impact_level, reason, recommended_actions: [], reminder_text, requires_user_confirmation }`          | 计划任务 2/3：个人影响评估     |
| `GET /api/agent/demo-flow`        | 入参：`user_id=demo_user&sample_mode=week2`；出参：`{ input_posts[], generated_events[], personal_impact, final_result }`                                                                                       | 计划任务 5：课堂演示（离线可用）   |

**关键说明：** 计划第 188-199 行定义了完整的 `PublicEvent`、`PublicEventSignal`、`PersonalImpactRequest`、`PersonalActionAdvice` 数据契约，前后端应按此契约对齐。

---

### 后端工作包 8：登录认证 API

**阻塞：FE-03 真实登录**

| 接口                     | 请求/响应字段                                                                                   |
| ---------------------- | ----------------------------------------------------------------------------------------- |
| `POST /api/auth/login` | 入参：`username`, `password`, `role`；出参：`token`, `user.id`, `user.role`, `user.display_name` |

---

### 管理员后台 API（第三周内容）

以下接口在飞书 FE-06~FE-09 中列出但**不在第二周计划范围内**，仅列出供第三周参考：

| 接口                                    | 作用         | 对应 FE |
| ------------------------------------- | ---------- | ----- |
| `GET /api/admin/overview`             | 后台概览统计     | FE-06 |
| `GET /api/admin/events`               | 事件审核列表     | FE-07 |
| `PATCH /api/admin/events/{id}/status` | 发布/驳回/归档   | FE-07 |
| `GET /api/admin/raw-posts`            | 原始帖子数据     | FE-08 |
| `GET /api/admin/processed-posts`      | 清洗后数据      | FE-08 |
| `GET /api/admin/feedbacks`            | 用户反馈列表     | FE-09 |
| `GET /api/admin/crawl-tasks`          | 爬虫任务状态     | FE-09 |
| `GET /api/admin/agent-runs`           | Agent 运行日志 | FE-09 |

---

## 五、第四部分：数据契约（来自第二周计划 §六）

计划第 606-703 行已定义了完整的接口契约，前端将严格按此实现：

### PublicEvent（计划第 614-630 行）

```json
{
  "id": "E-001",
  "title": "学校调整课间休息时间引发讨论",
  "summary": "多名学生讨论课间时间缩短后赶课压力增加。",
  "topic": "作息调整",
  "event_type": "schedule_adjustment",
  "sentiment": "negative",
  "risk_level": "medium",
  "heat_score": 0.78,
  "confidence": 0.82,
  "source_count": 12,
  "source_post_ids": ["weibo_001", "weibo_002"],
  "status": "pending_review",
  "created_at": "2026-05-27T09:00:00+08:00"
}
```

### PersonalActionAdvice（计划第 675-686 行）

```json
{
  "impact_level": "medium",
  "reason": "用户上午课程间隔较短，事件可能增加赶课风险。",
  "recommended_actions": [
    "提前 5 分钟离开上一节课教室",
    "将课前提醒提前到 15 分钟"
  ],
  "reminder_text": "明天上午课程间隔较短，建议提前出发。",
  "requires_user_confirmation": false
}
```

---

## 六、前端当前可独立推进的事项（无需等待后端）

以下事项**不依赖后端接口**，前端可以先完成 UI 和交互逻辑，等后端就绪后替换 API 调用：

| 事项                       | 说明                                                                                                    | 对应计划任务 | 状态    |
| ------------------------ | ----------------------------------------------------------------------------------------------------- | ------ | ----- |
| 创建 `src/api/http.js`     | 统一 axios 实例（含请求/响应拦截器、token注入、统一错误处理）                                                                 | FE-02  | ✅ 已完成 |
| 重写 `OpinionView.vue`     | 三栏布局（热点事件列表 / 事件研判详情 / Agent 问答），数据先用 mock                                                            | 计划任务 1 | ✅ 已完成 |
| 创建 `EventDetailView.vue` | 事件详情页 + "查看对我的影响"按钮 + 路由 `/events/:id`                                                                | 计划任务 3 | ✅ 已完成 |
| 改造 `PersonalView.vue`    | 新增"事件影响评估"区块，展示 impact_level / reason / recommended_actions / reminder_text，通过 `?event_id=xxx` 接收联动参数 | 计划任务 2 | ✅ 已完成 |
| 添加数据来源标识                 | 所有数据页面统一显示 DataSourceBadge（Mock 数据 / 离线 Demo / 实时接口）                                                  | 计划任务 4 | ✅ 已完成 |
| 编写演示路径文档                 | "首页→舆情→事件详情→个人影响→个人建议"的 3~5 分钟脚本                                                                      | 计划任务 5 | 无需完成？ |

### 已完成详情（2026-06-02）

**FE-02 `src/api/http.js`：**

- 统一 axios 实例，baseURL: `/api`
- 请求拦截器：自动注入 `Authorization: Bearer <token>`
- 响应拦截器：解包 `{ code, data, message }` 格式，`code !== 0` 自动抛出错误
- 401 → 自动登出跳转 `/login`，403 → 跳转 `/forbidden`
- 导出 `getDataSource()` / `setDataSource()` 供 DataSourceBadge 使用
- ⚠️ 拦截器依赖后端确认 `code` 枚举值和 401/403 格式后才能最终定稿

**计划任务 1 `OpinionView.vue`（重写）：**

- 三栏布局：左 280px（事件列表）/ 中 flex（研判详情）/ 右 320px（Agent 问答）
- 左栏：按热度排序的事件列表，前三名特殊高亮（红/橙/黄），点击切换详情
- 中栏：展示选中事件的标题、摘要、热度/置信度/来源/风险四项指标、话题/情感/来源帖子
- 右栏：Agent 对话面板，支持快捷提问和自由输入，mock 延迟模拟真实 Agent 响应
- "查看对我的影响"按钮 → 跳转 `/personal?event_id=xxx`
- DataSourceBadge 已集成

**计划任务 3 `EventDetailView.vue` + 路由：**

- 新文件 `src/views/EventDetailView.vue`
- 路由 `/events/:id` → `EventDetailView`（之前指向 EventListView）
- 展示：标题/风险等级/时间/状态、摘要、核心指标（热度/置信度/来源/代表内容/情感/话题）、来源平台/标签/来源帖子、7日热度趋势 SVG 图
- "查看对我的影响"按钮 → 跳转 `/personal?event_id=xxx`（核心联动链路）
- DataSourceBadge 已集成

**计划任务 2 `PersonalView.vue`（改造）：**

- 新增"⚠️ 事件影响评估"区块，通过路由 `?event_id=xxx` 参数控制显示
- 监听 `route.query.event_id` 变化，自动加载对应事件并生成 mock 影响数据
- 展示：影响等级（高/中/低，颜色区分）、影响分析原因、建议措施列表、提醒文案
- 三个风险等级各有独立 mock 数据（high/medium/low 对应不同场景）
- 联动链路：EventDetailView → PersonalView（?event_id=EVT-xxx）完整打通

**计划任务 4 DataSourceBadge：**

- 新组件 `src/components/DataSourceBadge.vue`
- 三种状态：`mock`（灰色 "Mock 数据"）、`demo`（黄色 "离线 Demo"）、`real`（不显示）
- 已集成到：HomeView、SentimentView、EventListView、OpinionView、EventDetailView、PersonalView

**联动链路验证：**

- ✅ 首页 → 事件列表 → 事件详情 → 个人影响评估，完整可走通（mock 数据）
- ✅ 舆情工作台 → Agent 问答 → 事件研判 → 个人影响评估，完整可走通（mock 数据）

---

## 七、总结

### 前端第二周核心阻塞点（按优先级）

1. **🔴 GET /events + GET /events/{id}** — 阻塞计划任务 1、3（舆情分析页 + 事件详情页）
2. **🔴 POST /agent/personal/impact** — 阻塞计划任务 2、3（个人影响评估 + 联动入口）
3. **🔴 GET /agent/demo-flow** — 阻塞计划任务 5（课堂演示离线链路）
4. **🟡 统一 API 返回结构** — 阻塞 FE-02（axios 拦截器和统一错误处理）
5. **🟡 POST /api/auth/login** — 阻塞 FE-03（替换 mock 登录为真实认证）

### 计划相关建议

1. **管理员后台（FE-06~FE-09）建议推迟第三周** — 第二周计划明确聚焦用户侧核心链路，管理员后台不在本周范围
2. **FE-04 不是"接入接口"而是"从零重建"** — 当前 OpinionView 是占位壳，需完整重写
3. **前端可提前用 mock 并行推进** — 不等后端，先用 mock 数据完成 UI 和交互逻辑，Day 12 联调时替换 API
4. **补入 FE-02（统一 axios）** — 虽计划未单独列出，但无统一请求层会导致联调时问题增多
